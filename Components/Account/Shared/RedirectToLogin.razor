@inject NavigationManager NavigationManager

@code {

    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo($"/login/admin?returnUrl={Uri.EscapeDataString(NavigationManager.Uri)}", true);
    }

}