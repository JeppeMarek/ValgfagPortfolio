@page "/"
@using Microsoft.IdentityModel.Tokens
@rendermode InteractiveServer
@inject ICategoryService categoryService

<PageTitle>Jeppe Marek Portfolio</PageTitle>
<AuthorizeView>
    <Authorized>
        <MudButton Size="Size.Large" Href="/category/new/" StartIcon="@Icons.Material.Filled.AddBox">Tilføj ny
            kategori
        </MudButton>
    </Authorized>
</AuthorizeView>
<MudGrid>
    <br/>
    @if (!categories.IsNullOrEmpty())
    {
        foreach (var cat in categories)
        {
            <MudItem sm="5">
                <CategoryComponent SelectedCategory=@cat></CategoryComponent>
            </MudItem>
        }
    }
    else
    {
        <MudText Typo="Typo.h6" Align="Align.Center" Color="Color.Warning">Ingen kategorier fundet..</MudText>
    }
</MudGrid>
